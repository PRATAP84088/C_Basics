#include<stdio.h>
int main(){
    // WAP to calculate area of a square.
   /* int length,breadth;
    printf("Enter length of the square : ");
    scanf("%d",&length);
    
    printf("Enter b : ");
    scanf("%d",&breadth);
    
    int area = length*breadth;
    printf("Area of the square is : %d", area);
            */


    //WAP to cal area of circle
    //          we know area of a circle is pi(R)^2
    float radius;
    printf("Enter radius : ");
    scanf("%d",&radius);
    
    printf("Area of the circle is : %f",3.14*radius*radius);

    //WAP to calculate perimeter of rectangle. take sides from user
    //take a number(n) from the user & output its  cube (n*n*n);
        return 0;
}
